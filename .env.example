# =============================================================================
# Claude-Mem Docker Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# AI Provider Configuration
# -----------------------------------------------------------------------------
# The worker service uses an AI model to compress and summarize observations.
# This is NOT for running Claude Code - it's for the memory compression feature.
# You need ONE of the following API keys:

# Option 1: Anthropic Claude API
# ANTHROPIC_API_KEY=

# Option 2: Google Gemini
# CLAUDE_MEM_GEMINI_API_KEY=

# Option 3: OpenRouter (has free models)
# CLAUDE_MEM_OPENROUTER_API_KEY=

# Option 4: Ollama (fully local, no API costs - recommended)
# No API key needed - just set the URL if not using default
# CLAUDE_MEM_OLLAMA_URL=http://localhost:11434

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------

# AI provider: claude | gemini | openrouter | ollama
CLAUDE_MEM_PROVIDER=ollama

# Model to use for compression (depends on provider)
# Ollama model (default): llama3.2
# OpenRouter free model: xiaomi/mimo-v2-flash:free
# Claude model: claude-sonnet-4-5
CLAUDE_MEM_OLLAMA_MODEL=llama3.2

# -----------------------------------------------------------------------------
# Service Configuration
# -----------------------------------------------------------------------------

# Worker HTTP port (exposed to host)
CLAUDE_MEM_WORKER_PORT=37777

# Full worker URL override (for container/remote deployments)
# If set, overrides host+port construction
# Examples:
#   - Container with port forwarding: http://localhost:37777 (default)
#   - Remote worker: http://192.168.1.100:37777
#   - Docker for Mac/Windows (inside container): http://host.docker.internal:37777
# CLAUDE_MEM_WORKER_URL=

# Log level: DEBUG | INFO | WARN | ERROR
CLAUDE_MEM_LOG_LEVEL=INFO
